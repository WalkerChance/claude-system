# Transcript: TpKhqbeeDbk
# Channel: unknown
# Video ID: TpKhqbeeDbk
# URL: https://www.youtube.com/watch?v=TpKhqbeeDbk

Hi, my name is Kitha. Today I'm demonstrating my job screenshot agent, an automated workflow designed to eliminate repetitive manual effort in job tracking and market research. So typically collecting job data involves uh opening multiple links, waiting for pages to load and manually saving screenshot. So this becomes uh inefficient especially when dealing with broken or restricted links. So to solve this I build an agent that works directly from an Excel input file. So here is the input file option one job links. We need to open it. So it contains five job postings with their URLs. Importantly, one of these link is intentionally broken. So, I included this to demonstrate how the system handles the real world unrelable data. So, the agent does not rely on any pre-labelled columns like status. So, it reads every URL as unknown input. And the next step is execution part. So now we need to open command prompt and type code space dot. It opens the VS code. So to execute I'll run some commands. First of all we need to uh install all the requirements which are needed to this project. So and the next step uh we need to install chromium like play rate chromium. So next we need to run the agent. So in the background it launches a headless chromium browser using play rate. Instead of scraping which can fail due to dynamic content it behaves like re real users like load each pages and captures a full image screenshot. So as you can see in the logs uh the first few urls are processed successfully. Now it encounters the broken link. So here instead of crashing the agent catches the error locks it clearly and continuous process the remaining URLs. So this makes the system fault or lent and reliable for real world usage. So and next it sends email to uh the receiver. So who are receiver who are receiving this proper uh output so they will get received. So now let's look at the output directory. So we have four full page screenshots corresponding to the valid job posting. So each screenshot captures the exact UI as seen by users ensuring accuracy even for dynamic or protected pages. So here these are the four screenshots we have seen and the final step is automated delivery. So the agent compiles all successful screenshots and send them directly via email. So in this email all the screenshots are captured attached and the summary report is included. It clearly shows which URL is failed and the reason. So instead of manually verifying each link I receive a complete structure result instantly. So now we can click on each URL and we can see what it it's going through. So in the first URL we can see uh what are the contents whatever the job contents we have seen and the next second URL uh in the Amazon jobs and third URL it uh it describes about the Figma job presentations and the final URL We need to sign in to create a new LinkedIn account. So again open the previous URL we can see. So it shows like no longer accepting applications. It is open but it is having some other issues. So the next it goes to failed URL. It shows like this sign can't be reached. So it it it means it is not working. So So here is the four attachment the screenshots. This project demonstrates how an agentic system can autonomously execute a multi-step workflow from data injection to the browser interaction, error handling and the final delivery. So making the process very efficient, scalable and reliable. Thank you.